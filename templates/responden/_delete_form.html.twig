<form method="post" action="{{ path('responden_delete', {'id': responden.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ responden.id) }}">
    <button class="btn">Delete</button>
</form>
