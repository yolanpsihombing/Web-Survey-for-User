{% extends 'base.html.twig' %}

{% block title %}Instansi index{% endblock %}

{% block body %}
    <h1>Instansi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nama</th>
                <th>Alamat</th>
                <th>Nomor_kontak</th>
                <th>Nama_pimpinan</th>
                <th>Tingkat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for instansi in instansis %}
            <tr>
                <td>{{ instansi.id }}</td>
                <td>{{ instansi.nama }}</td>
                <td>{{ instansi.alamat }}</td>
                <td>{{ instansi.nomorKontak }}</td>
                <td>{{ instansi.namaPimpinan }}</td>
                <td>{{ instansi.tingkat }}</td>
                <td>
                    <a href="{{ path('instansi_show', {'id': instansi.id}) }}">show</a>
                    <a href="{{ path('instansi_edit', {'id': instansi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('instansi_new') }}">Create new</a>
{% endblock %}
