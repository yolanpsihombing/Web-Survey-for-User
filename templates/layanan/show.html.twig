{% extends 'base.html.twig' %}

{% block title %}Layanan{% endblock %}

{% block body %}
    <h1>Layanan</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ layanan.id }}</td>
            </tr>
            <tr>
                <th>Nama</th>
                <td>{{ layanan.nama }}</td>
            </tr>
            <tr>
                <th>Deskripsi</th>
                <td>{{ layanan.deskripsi }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('layanan_index') }}">back to list</a>

    <a href="{{ path('layanan_edit', {'id': layanan.id}) }}">edit</a>

    {{ include('layanan/_delete_form.html.twig') }}
{% endblock %}
