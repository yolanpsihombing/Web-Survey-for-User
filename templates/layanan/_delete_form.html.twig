<form method="post" action="{{ path('layanan_delete', {'id': layanan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ layanan.id) }}">
    <button class="btn">Delete</button>
</form>
